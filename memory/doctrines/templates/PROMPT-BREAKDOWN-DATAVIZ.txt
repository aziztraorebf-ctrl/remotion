# TEMPLATE — Prompt de BREAKDOWN data-viz (a donner a GPT-5.5 vision avec le panneau storyboard).
# Genere/prouve 2026-06-20. Adapter la description du sujet (ici exemple "70%% phosphate Maroc").
# Doctrine : WORKFLOW-DATAVIZ.md etape 3. Outil : openrouter-vision-breakdown.py --model openai/gpt-5.5.
# ----------------------------------------------------------------------------------------------

Tu es directeur technique d'une chaine de data-visualization premium (style Bloomberg/Vox).
On te donne UN PANNEAU de storyboard (etat final d'un beat "70%" : le Maroc detient 70% des
reserves mondiales de phosphate). Ce panneau EST LA CIBLE VISUELLE A REPRODUIRE A ~100% dans
une video Remotion (React) horizontale 1920x1080, 30fps.

Notre methode (NON-NEGOTIABLE) :
- Le storyboard n'est PAS un brouillon a reinterpreter : c'est le livrable a reproduire fidelement.
- Ce qui est BEAU et FIGE (matiere, relief, fond, illustrations) = on le GENERE en image (Gemini/Recraft,
  fond transparent), on ne le redessine PAS a la main en SVG/CSS (ca donne du plat amateur).
- Ce qui BOUGE / COMPTE / se met en scene = REMOTION (placement, animation, count-up, timing audio).
- Certains elements sont HYBRIDES : un asset genere + une couche Remotion par-dessus (ex un chiffre
  dont la MATIERE est generee mais qui doit faire un count-up anime).

⛔ IGNORE les ANNOTATIONS DE STORYBOARD : titre d'etat (ex "ETAT 3 — L'ecrasement"), timer/chrono (ex "00:08"),
numero de case. Ce sont des echafaudages du storyboard, PAS du contenu a reproduire. NE les inclus PAS dans la
table d'elements (sinon le codeur les reproduit a tort dans le render final).

TON TRAVAIL : decouper CE panneau en une table EXHAUSTIVE d'elements (HORS annotations). Pour CHAQUE element visible,
TRANCHE toi-meme (ne laisse RIEN a deviner a celui qui codera) :

Retourne UNIQUEMENT du JSON (pas de markdown), avec cette structure :
{
  "panneau": "etat 3 - l'ecrasement",
  "ratio": "16:9",
  "palette": { "or_clair": "#...", "or_fonce": "#...", "fond_teal": "#...", "accent_fleche": "#...", "...": "..." },
  "background": { "verdict": "GENERE|REMOTION|HYBRIDE", "description": "...",
    "asset_prompt": "<si GENERE/HYBRIDE : le prompt EXACT pour reproduire ce fond a 100%, en precisant
       style (flat editorial illustration, PAS realiste), couleurs hex, fond, dimensions 1920x1080>",
    "remotion": "<si REMOTION/HYBRIDE : ce que Remotion fait (ex vignettage anime, grille)>" },
  "elements": [
    {
      "id": "chiffre-70",
      "quoi": "le '70' geant dore avec faux-relief 3D",
      "verdict": "GENERE|REMOTION|HYBRIDE",
      "asset_prompt": "<si GENERE/HYBRIDE : prompt EXACT pour generer cet asset a 100% (matiere or,
         degrade, relief, fond TRANSPARENT, style flat illustration, taille). Ecris le prompt pret a copier-coller.>",
      "remotion": "<si REMOTION/HYBRIDE : ce que Remotion code par-dessus/a la place (ex count-up 0->70,
         bounce overshoot a la fin, position x/y, scale d'entree)>",
      "style_exact": "<OBLIGATOIRE pour tout element REMOTION/HYBRIDE qui est un TEXTE, un TRAIT, une FLECHE,
         un CADRE/CARTOUCHE, une BARRE : la MATIERE exacte a coder, rien laisse au codeur. Pour un TEXTE :
         police nommee (ex 'Bebas Neue' / 'Anton' / 'Oswald', condensee/bold) + couleur hex + taille en % du
         cadre OU px@1080 + ombre/contour exacts. Pour un TRAIT/FLECHE : couleur hex + epaisseur en px@1080 +
         style de pointe (fine/triangle plein) + opacite. Pour un CADRE : couleur bordure hex + epaisseur +
         fond hex+opacite + rayon. NE METS PAS 'doree' / 'epais' : donne la valeur EXACTE mesuree sur la cible.>",
      "intention": "<PROSE : ce que cet element doit faire RESSENTIR a ce moment (pas un code, une phrase)>",
      "position": "<ou dans le cadre 16:9 : ex 'centre-gauche, occupe 45% largeur'>",
      "sync_voix": "<le mot de la narration sur lequel il apparait/agit>"
    }
    // ... UN objet par element : fond, chiffre 70, signe %, picto Terre+phosphate 'RESERVES MONDIALES',
    //     barre 'PHOSPHATE' + segment '30% RESTE DU MONDE', source USGS, labels, traits de liaison.
  ],
  "ce_qui_reste_a_remotion": "<SYNTHESE en clair : une fois TOUS les assets generes, la liste de ce
     qu'il reste a faire dans Remotion (placement, animations, count-up, barre qui s'ecrase, transitions,
     timing). Pour qu'un agent frais sache exactement son perimetre de code sans rien deviner.>",
  "assets_a_generer": ["<liste des ids verdict=GENERE ou HYBRIDE, = la checklist de generation>"],
  "intention_globale_du_panneau": "<PROSE 2-3 phrases : l'arc de cet etat, ce qu'on veut que le spectateur ressente>"
}

REGLES sur les asset_prompt :
- Style TOUJOURS 'flat editorial illustration', JAMAIS realiste/photo (sinon non reproductible et dur a manipuler).
- Toujours preciser 'fond transparent' pour les assets a superposer (le chiffre, le picto, la barre).
- Le fond pleine page peut etre opaque.
- Donne des prompts PRETS A L'EMPLOI (couleurs hex exactes vues dans l'image, pas de "couleur doree" vague).
- Sois exhaustif : chaque chose visible a un verdict. Ne regroupe pas, ne saute rien.
- ⛔ CALIBRE L'INTENSITE des effets a "JUSTE ASSEZ" (premium editorial = sobre). Pour un relief/extrusion 3D :
  precise "extrusion SUBTILE, faible profondeur, editoriale, PAS un gros biseau cartoon/jeu video". Le premium
  vient de la RETENUE, pas de l'exageration. Decris l'intensite exacte (ex "profondeur d'extrusion ~8% de la
  hauteur du glyphe", "ombre douce et courte"). Mieux vaut trop sobre que trop ressorti.

⛔ REGLES sur le champ "remotion" (CRITIQUE pour un centrage/placement juste sans deviner) :
- Notre stack = Remotion + TAILWIND CSS. Donne le placement en TAILWIND + Remotion, JAMAIS en pixels d'une
  vignette de storyboard. Le render cible est 1920x1080.
- Exprime les positions en : classes Tailwind de centrage/flux (`absolute left-1/2 top-1/2 -translate-x-1/2
  -translate-y-1/2`, `flex items-center justify-center`, `gap-x`, etc.) OU en POURCENTAGE du cadre 1920x1080
  (ex "centre vertical, ancre horizontale a 40% de la largeur"). JAMAIS "x=530 y=265" (illisible/faux hors vignette).
- Donne la TAILLE cible de chaque element en % du cadre (ex "le 70 occupe ~50% de la hauteur du cadre") pour
  qu'un agent dimensionne juste, pas en px de vignette.
- Precise l'animation Remotion (spring damping/stiffness, count-up, crossfade) ET l'ancrage Tailwind ensemble.
- But : un agent code le placement DIRECTEMENT depuis le champ remotion, en Tailwind, sans rien recalculer ni deviner.

⛔⛔ REGLE "PICTO + LABEL SECONDAIRE — NE PAS LES SOUS-DIMENSIONNER" (faiblesse recurrente n1, prouvee 2x
le 2026-06-20 : mon cobaye ET un agent vierge ont sorti le picto + label "RESERVES MONDIALES" trop petit).
CAUSE RACINE MESUREE : tu (le modele de breakdown) SOUS-MESURES systematiquement les elements secondaires
petits et en marge (picto illustre + son label), alors que le chiffre hero et la barre, eux, sont bien
dimensionnes. Le biais vient du bandeau d'annotation du storyboard qui ecrase l'echelle percue.
PARADE OBLIGATOIRE — pour TOUT picto illustratif secondaire (globe, icone, embleme) ET son label texte :
- MESURE sa taille en % du cadre en IGNORANT le bandeau d'annotation (titre d'etat + timer), exactement comme
  pour les autres elements.
- PUIS donne la taille cible avec une MARGE DE SECURITE de +40% sur ta premiere estimation (ex tu mesures
  "picto ~11% de largeur" -> ecris "w-[15%] a w-[16%]"). Mieux vaut un picto legerement trop grand (corrigeable
  d'un coup d'oeil) que riquiqui (= trahit l'amateurisme, c'est l'element n1 qui le revele).
- AJOUTE dans le champ "remotion"/"style_exact" de CE picto et de CE label la mention explicite :
  "TAILLE PLANCHER — verifier en PLEIN FORMAT que le picto+label ont une PRESENCE (pas riquiqui). Seuil
   minimum : picto >= 13% de largeur du cadre, label secondaire >= 40px @1080."
- Un seuil PLANCHER s'applique meme si la cible parait montrer plus petit : picto secondaire >= 13% largeur,
  label secondaire >= 40px @1080. En-dessous = systeme rate par defaut, NE PAS descendre sous le plancher.

⛔⛔ REGLE "ZERO CHOIX LAISSE AU CODEUR" (cause prouvee n1 des divergences cible-vs-render, 2026-06-20) :
Un element REMOTION (texte/trait/fleche/cadre/barre) dont tu donnes la position+taille MAIS PAS la matiere force
le codeur a DEVINER police/hex/epaisseur → il devine differemment a chaque fois → le render "ressemble" sans etre
"identique" (vu : fleches mauvaise couleur, cadre PHOSPHATE different, label mauvaise fonte). REMPLIS `style_exact`
pour CHAQUE element REMOTION/HYBRIDE textuel ou graphique. Le critere de qualite : deux codeurs differents qui
suivent ce breakdown produisent le MEME pixel. Si tu hesites sur une valeur, MESURE-la sur la cible, ne l'omets pas.
- POLICES : nomme une police reelle et disponible (Bebas Neue, Anton, Oswald, Archivo Black, Impact). Pour les
  chiffres/titres condensses bold de ce style, defaut = 'Bebas Neue' sauf si la cible montre clairement autre chose.
- Donne aussi une PALETTE GLOBALE en tete (champ "palette" : les 4-6 hex dominants de la cible : or clair, or
  fonce/brun, teal fond, accent fleche...) que tous les style_exact reutilisent — coherence garantie.
